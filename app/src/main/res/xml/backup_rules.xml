<?xml version="1.0" encoding="utf-8"?>
<!-- Legacy (API < 31) counterpart of data_extraction_rules.xml: include-only,
     so the ops database and encrypted key file are never backed up. -->
<full-backup-content>
    <include domain="database" path="blipbird-user.db" />
    <include domain="file" path="datastore/settings.preferences_pb" />
</full-backup-content>
